# Dev-only mosquitto config. Allows anonymous connections so we don't
# have to manage a password file on the dev host. DO NOT use in prod —
# the prod config under mosquitto/config/ requires auth.

persistence true
persistence_location /mosquitto/data/
log_dest stdout

listener 1883 0.0.0.0
allow_anonymous true
